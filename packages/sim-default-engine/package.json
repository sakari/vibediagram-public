{
  "name": "@diagram/sim-default-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run",
    "test:check": "vitest run --reporter dot --bail 1 --coverage",
    "test:watch": "vitest",
    "bench": "vitest bench",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@diagram/sim-model": "workspace:^",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "@vitest/coverage-v8": "^3.2.4",
    "fast-check": "^4.5.3",
    "typescript": "^5.2.2",
    "vitest": "^3.0.0"
  }
}
