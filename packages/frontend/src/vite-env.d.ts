/// <reference types="vite/client" />

/** Vercel deployment ID injected at build time; defaults to "local" */
declare const __DEPLOYMENT_ID__: string;
/** Vercel deployment URL injected at build time; defaults to "" */
declare const __DEPLOYMENT_URL__: string;
